# Generation Use Case

The Generation use case covers the process of instantiating a plan, whether by capturing elements in an explicitly authored plan, with or without automated support, or as an externally defined query, as in a C-CDA document. 

## Overview

